import { Injectable } from '@angular/core';
import { Subject } from 'rxjs';

@Injectable({
  providedIn: 'root'
})
export class NavStateService {

  // private _currentPage: Subject<string> = new Subject();
  // private currentPage = this._currentRoute.asObservable();

  constructor() {
  }
}
